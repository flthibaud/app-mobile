{
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,front -c blue,green \"PHP_CLI_SERVER_WORKERS=6 php -S 0.0.0.0:8000 -t api/public\" \"pnpm --dir frontend dev\"",
    "api": "PHP_CLI_SERVER_WORKERS=6 php -S 0.0.0.0:8000 -t api/public",
    "front": "pnpm --dir frontend dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
